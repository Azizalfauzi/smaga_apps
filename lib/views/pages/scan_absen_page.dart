part of 'pages.dart';

class ScanAbsenPage extends StatelessWidget {
  const ScanAbsenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Scan"),
      ),
    );
  }
}
